# EXTENDING POTGRES IMAGES
FROM postgres:latest

# POSTGRES VARIABLES
ENV POSTGRES_PASSWORD=1234
ENV POSTGRES_USER=admin
ENV POSTGRES_DB=blogs_db

# PORT EXPOSED
EXPOSE 5432/tcp

# INITIALIZE VALUES
COPY schema.sql /docker-entrypoint-initdb.d/schema.sql



